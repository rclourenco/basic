x = 0
input "Introduza um numero", x

' isto e um comentario
if x=0 then
	print "Eq", x, "= 0"
else
	if x > 0 then
		print "Sup", x, "> 0"
	else
		print "Inf", x, "< 0"
	end
end

' isto e um comentario

for i = 0 to 100 step .90
	print "hello"
next

for i = 0 to 100
	print "world"
	for j=1 to 10
		print "aaaa";
	next
	print a+cos(x)*12
next

